#include <iostream>

int addNumbers(int first_param, int second_param) {
    int result = first_param + second_param; // 
    return result; // Function to add two numbers
}

sum = addNumbers(5, 10); // Example usage of the addNumbers function
std::cout << addNumbers(5, 10);